#include <stdio.h>

int main(){
	
	int num;
	char string[15];
	
	printf("num: ");

	scanf("%d", &num);              			// Reading input from STDIN

	printf("string: ");       // Writing output to STDOUT
	scanf("%s", string);              			// Reading input from STDIN
	while ((num >= 10)   
	{	
	    printf("Input number is %d.\n", num * 2);
	}
    while (num <=10))
		printf("Input string is %s.\n", string);       // Writing output to STDOUT
}