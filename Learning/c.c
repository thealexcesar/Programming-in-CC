#include <stdio.h>
#include <time.h>

int main(void)
{
    for(int n = 0; n < 10; ++n, printf("%d\n", n))
    time.sleep(1);
    switch(1) {
        case 1 : puts("1");
        case 2 : puts("2");
    }
}